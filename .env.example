# ===========================================
# Root Environment Configuration
# ===========================================
# This file is used by docker-compose and CI/CD.
# Copy this to .env and fill in your values.

# ===========================================
# Required: Secrets (never commit real values!)
# ===========================================

# JWT Secrets - generate with: openssl rand -base64 32
JWT_ACCESS_SECRET=your-access-secret-change-in-production
JWT_REFRESH_SECRET=your-refresh-secret-change-in-production

# Anthropic Claude API Key
# Get your key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-key-here

# ===========================================
# BUYBOX Integration (Optional)
# ===========================================
# Required only if you want real purchase history tracking

# Widget ID from BUY.BOX publisher panel
BUYBOX_WIDGET_ID=your-widget-id

# API Token for Transactions API
BUYBOX_API_TOKEN=your-api-token

# Space ID (publisher account identifier)
BUYBOX_SPACE_ID=your-space-id

# ===========================================
# Database (for docker-compose)
# ===========================================

# PostgreSQL credentials (used by docker-compose)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=szukaj_ksiazek

# Full database URL (auto-constructed in docker-compose, set manually for prod)
# DATABASE_URL=postgresql://user:password@host:5432/dbname?schema=public

# ===========================================
# Application Settings
# ===========================================

# Environment: development | production
NODE_ENV=development

# API Port
PORT=3000

# CORS Origins (comma-separated)
# For production, replace with your actual frontend domains
CORS_ORIGINS=http://localhost:8081,http://localhost:19006,http://localhost:3000

# ===========================================
# Notes
# ===========================================
# 1. Copy this file to .env before running docker-compose
# 2. Generate secure JWT secrets for production
# 3. Never commit your actual .env file with real secrets
# 4. For production deployment, set these as environment variables
#    in your hosting platform (Railway, Fly.io, Render, etc.)
