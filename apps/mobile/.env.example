# ===========================================
# Mobile App Configuration
# ===========================================

# API Base URL - the backend API endpoint
# For local development with Expo on a physical device, use your machine's local IP
# For iOS Simulator: http://localhost:3000/api/v1
# For Android Emulator: http://10.0.2.2:3000/api/v1
# For production: https://api.yourdomain.com/api/v1
EXPO_PUBLIC_API_URL=http://localhost:3000/api/v1

# ===========================================
# Optional: EAS Build Configuration
# ===========================================
# These are only needed if you're using Expo EAS Build

# EAS Project ID (from app.json or eas.json)
# EXPO_PUBLIC_EAS_PROJECT_ID=your-eas-project-id

# ===========================================
# Optional: Analytics / Monitoring
# ===========================================
# Add your analytics keys here when ready

# Sentry DSN for error tracking (optional)
# EXPO_PUBLIC_SENTRY_DSN=https://xxx@sentry.io/xxx

# ===========================================
# Notes
# ===========================================
# 1. All env vars must be prefixed with EXPO_PUBLIC_ to be accessible in the app
# 2. Never commit actual API keys or secrets - use this file as a template
# 3. Create a .env file (not committed) with your actual values
